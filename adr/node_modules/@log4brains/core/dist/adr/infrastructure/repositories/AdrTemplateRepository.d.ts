import { AdrTemplateRepository as IAdrTemplateRepository } from "@src/adr/application";
import { AdrTemplate, PackageRef } from "@src/adr/domain";
import { Log4brainsConfig } from "@src/infrastructure/config";
import { PackageRepository } from "./PackageRepository";
declare type Deps = {
    config: Log4brainsConfig;
    workdir: string;
    packageRepository: PackageRepository;
};
export declare class AdrTemplateRepository implements IAdrTemplateRepository {
    private readonly config;
    private readonly workdir;
    private readonly packageRepository;
    constructor({ config, workdir, packageRepository }: Deps);
    find(packageRef?: PackageRef): Promise<AdrTemplate>;
    private getAdrFolderPath;
}
export {};
//# sourceMappingURL=AdrTemplateRepository.d.ts.map