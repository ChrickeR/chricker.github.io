import { ValueObject } from "@src/domain";
declare type Props = {
    name: string;
};
export declare class AdrStatus extends ValueObject<Props> {
    static DRAFT: AdrStatus;
    static PROPOSED: AdrStatus;
    static REJECTED: AdrStatus;
    static ACCEPTED: AdrStatus;
    static DEPRECATED: AdrStatus;
    static SUPERSEDED: AdrStatus;
    private constructor();
    get name(): string;
    static createFromName(name: string): AdrStatus;
}
export {};
//# sourceMappingURL=AdrStatus.d.ts.map