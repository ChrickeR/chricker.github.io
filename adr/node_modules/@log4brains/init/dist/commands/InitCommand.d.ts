import type { AppConsole } from "@log4brains/cli-common";
export declare type InitCommandOpts = {
    defaults: boolean;
};
declare type Deps = {
    appConsole: AppConsole;
};
export declare class InitCommand {
    private readonly console;
    constructor({ appConsole }: Deps);
    private guessMainAdrFolderPath;
    private buildLog4brainsConfigInteractively;
    private createAdr;
    private copyFileIfAbsent;
    private printSuccess;
    private askPathWhileNotFound;
    /**
     * Command flow.
     *
     * @param options
     * @param customCwd
     */
    execute(options: InitCommandOpts, customCwd?: string): Promise<void>;
}
export {};
//# sourceMappingURL=InitCommand.d.ts.map