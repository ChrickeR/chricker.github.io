import { Log4brains } from "@log4brains/core";
import type { AppConsole } from "@log4brains/cli-common";
declare type Deps = {
    l4bInstance: Log4brains;
    appConsole: AppConsole;
};
export declare type NewCommandOpts = {
    quiet: boolean;
    package?: string;
    from?: string;
};
export declare class NewCommand {
    private readonly l4bInstance;
    private readonly console;
    constructor({ l4bInstance, appConsole }: Deps);
    private detectCurrentPackageFromCwd;
    execute(opts: NewCommandOpts, titleArg?: string): Promise<void>;
}
export {};
//# sourceMappingURL=NewCommand.d.ts.map