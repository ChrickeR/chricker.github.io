import { Adr, MarkdownAdrLink, MarkdownAdrLinkResolver as IMarkdownAdrLinkResolver } from "@src/adr/domain";
import type { AdrRepository } from "./repositories";
declare type Deps = {
    adrRepository: AdrRepository;
};
export declare class MarkdownAdrLinkResolver implements IMarkdownAdrLinkResolver {
    private readonly adrRepository;
    constructor({ adrRepository }: Deps);
    resolve(from: Adr, uri: string): Promise<MarkdownAdrLink | undefined>;
}
export {};
//# sourceMappingURL=MarkdownAdrLinkResolver.d.ts.map