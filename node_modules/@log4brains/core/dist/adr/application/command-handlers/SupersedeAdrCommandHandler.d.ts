import { CommandHandler } from "@src/application";
import { SupersedeAdrCommand } from "../commands";
import { AdrRepository } from "../repositories";
declare type Deps = {
    adrRepository: AdrRepository;
};
export declare class SupersedeAdrCommandHandler implements CommandHandler {
    readonly commandClass: typeof SupersedeAdrCommand;
    private readonly adrRepository;
    constructor({ adrRepository }: Deps);
    execute(command: SupersedeAdrCommand): Promise<void>;
}
export {};
//# sourceMappingURL=SupersedeAdrCommandHandler.d.ts.map